Notes:

- Data store uses `Map`, which could result in memory leaks. An LRU mechanism could prune unused baskets although this could also lead to unintended effects for the user downstream

- FORMAT WITH PRETTIER WHEN downstream
- TIDY UP ROUTER AND TEST SUITE